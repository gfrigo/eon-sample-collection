# Dependências do sistema de captura de amostras biológicas

# Processamento de imagens e captura de vídeo
opencv-python==4.9.0.80

# Controle de portas GPIO do Raspberry Pi
# RPi.GPIO==0.7.0 nao compila no Raspberry Pi OS Bookworm (gcc -fno-common).
# rpi-lgpio e' um substituto direto (mesmo namespace "RPi.GPIO"), baseado em
# lgpio (ja incluso no SO) e sem extensao C para compilar.
rpi-lgpio

# LCD I2C 16x2 (PCF8574)
RPLCD==1.3.1

# Google Cloud Storage
google-cloud-storage>=2.16
google-auth>=2.29

# Inferencia local do modelo (MobileNetV2 + TFLite, treinado em eon-aab-ml-train)
# ai-edge-litert e' o sucessor mantido do tflite-runtime (descontinuado em 2021)
# e suporta os ops mais novos gerados pelas versoes atuais do TensorFlow.
# Tem wheel aarch64 (Raspberry Pi OS 64-bit) direto no PyPI.
ai-edge-litert; platform_machine == "aarch64" or platform_machine == "armv7l"
Pillow>=10.0
numpy>=1.24,<2
